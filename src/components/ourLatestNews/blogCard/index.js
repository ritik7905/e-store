import React from "react";

const BlogCard = ({ blogCard }) => {
  // console.log(blogCard, "=================");

  return (
    <div className="blog-card">
      <div className="blog-card-body">hello-card</div>
    </div>
  );
};

export default BlogCard;
